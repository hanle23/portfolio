import type { AudioFeaturesObject } from '@/app/types/spotify/audioFeatures'
export default function calculateFeatureSimilarity(
  playlistTrackFeatures: AudioFeaturesObject[],
  trackFeature: AudioFeaturesObject,
): number {
  return 0
}
